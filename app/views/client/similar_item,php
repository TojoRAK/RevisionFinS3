<!doctype html>
<html lang="fr" data-theme="dark">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Objets similaires — Takalo-takalo</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="../assets/css/theme.css" rel="stylesheet">
</head>

<body class="tt-page">

    <?php include('inc/header.php') ?>

    <main class="tt-main">
        <div class="container py-4">

            <!-- HERO -->
            <div class="tt-hero p-3 p-md-4 mb-3">
                <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
                    <div>
                        <h1 class="h4 mb-1">Objets à prix similaires</h1>
                        <div class="text-muted-2">
                            Résultats correspondant à ton objet :
                            <span class="badge badge-soft">“<?= htmlspecialchars($baseObjet['title'] ?? 'Mon objet') ?>”</span>
                            <span class="badge badge-soft">
                                <?= number_format($baseObjet['estimated_value'] ?? 0, 0, ',', ' ') ?> Ar
                            </span>
                            <span class="badge badge-soft-info">
                                ±<?= (int)($pct ?? 10) ?>%
                            </span>
                        </div>
                    </div>

                    <!-- FILTRES (UI) -->
                    <div class="d-flex gap-2 flex-wrap">
                        <a class="btn btn-tt-ghost"
                            href="/my-objets">
                            <i class="bi bi-arrow-left me-1"></i>Retour
                        </a>

                        <div class="input-group" style="min-width: 260px;">
                            <span class="input-group-text bg-transparent"
                                style="border-color:var(--tt-border); color:var(--tt-text)">
                                <i class="bi bi-search"></i>
                            </span>
                            <input class="form-control" name="q" value="<?= htmlspecialchars($_GET['q'] ?? '') ?>"
                                placeholder="Rechercher dans les résultats…">
                        </div>

                        <select class="form-select" name="category_id">
                            <option value="">Toutes catégories</option>
                            <?php foreach (($categories ?? []) as $cat): ?>
                                <option value="<?= $cat['id'] ?>"
                                    <?= (isset($_GET['category_id']) && $_GET['category_id'] == $cat['id']) ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($cat['name']) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>

                        <!-- % range quick links -->
                        <div class="btn-group">
                            <a class="btn btn-tt-ghost"
                                href="?pct=10<?= isset($_GET['category_id']) ? '&category_id=' . urlencode($_GET['category_id']) : '' ?>">
                                ±10%
                            </a>
                            <a class="btn btn-tt-ghost"
                                href="?pct=20<?= isset($_GET['category_id']) ? '&category_id=' . urlencode($_GET['category_id']) : '' ?>">
                                ±20%
                            </a>
                        </div>

                        <button class="btn btn-tt-ghost" type="submit" form="filtersForm" style="display:none;"></button>
                    </div>
                </div>

                <!-- Petit résumé min/max -->
                <?php
                $baseVal = (float)($baseObjet['estimated_value'] ?? 0);
                $p = ((int)($pct ?? 10)) / 100;
                $minVal = (int)round($baseVal * (1 - $p));
                $maxVal = (int)round($baseVal * (1 + $p));
                ?>
                <div class="text-muted-2 small mt-3">
                    <i class="bi bi-sliders"></i>
                    Intervalle visé : <span class="badge badge-soft"><?= number_format($minVal, 0, ',', ' ') ?> Ar</span>
                    →
                    <span class="badge badge-soft"><?= number_format($maxVal, 0, ',', ' ') ?> Ar</span>
                </div>
            </div>

            <!-- LIST -->
            <div class="tt-surface p-3 p-md-4">
                <?php if (empty($objets)): ?>
                    <div class="text-center py-5">
                        <i class="bi bi-inbox fs-1 text-muted"></i>
                        <div class="mt-2 text-muted">Aucun objet similaire trouvé</div>
                        <div class="small text-muted-2">Essaie de changer la catégorie ou le ±10% / ±20%.</div>
                    </div>
                <?php else: ?>

                    <div class="row g-3">
                        <?php foreach ($objets as $objet): ?>
                            <div class="col-12 col-md-6 col-lg-4">
                                <div class="tt-card p-3 h-100 d-flex flex-column">

                                    <!-- Image -->
                                    <div class="mb-3">
                                        <?php
                                        $imagePath = !empty($objet['main_image'])
                                            ? htmlspecialchars($objet['main_image'])
                                            : '../assets/img/placeholder.jpg';
                                        $imageCount = $objet['image_count'] ?? 0;
                                        ?>
                                        <div class="position-relative">
                                            <img src="<?= $imagePath ?>"
                                                alt="<?= htmlspecialchars($objet['title']) ?>"
                                                class="w-100 rounded"
                                                style="aspect-ratio: 4/3; object-fit: cover; border: 1px solid var(--tt-border)">
                                            <?php if ($imageCount > 1): ?>
                                                <span class="badge badge-soft position-absolute top-0 end-0 m-2">
                                                    <i class="bi bi-images"></i> <?= (int)$imageCount ?>
                                                </span>
                                            <?php endif; ?>
                                        </div>
                                    </div>

                                    <!-- Title -->
                                    <div class="fw-semibold mb-1">
                                        <?= htmlspecialchars($objet['title']) ?>
                                    </div>

                                    <!-- Meta row -->
                                    <div class="d-flex flex-wrap gap-2 mb-2">
                                        <span class="badge badge-soft">
                                            <i class="bi bi-tag"></i> <?= htmlspecialchars($objet['category_name'] ?? '—') ?>
                                        </span>
                                        <span class="badge badge-soft">
                                            <?= number_format($objet['estimated_value'], 0, ',', ' ') ?> Ar
                                        </span>
                                    </div>

                                    <!-- Description -->
                                    <div class="mb-3 text-muted-2 small flex-grow-1">
                                        <?php
                                        $description = $objet['description'] ?? '';
                                        echo nl2br(htmlspecialchars(substr($description, 0, 80)));
                                        if (strlen($description) > 80) echo '...';
                                        ?>
                                    </div>

                                    <!-- Owner + Date (optionnel) -->
                                    <div class="text-muted-2 small mb-3 d-flex justify-content-between">
                                        <span><i class="bi bi-person"></i> <?= htmlspecialchars($objet['owner_name'] ?? 'Utilisateur') ?></span>
                                        <span><i class="bi bi-calendar3"></i> <?= !empty($objet['created_at']) ? date('d/m/Y', strtotime($objet['created_at'])) : '—' ?></span>
                                    </div>

                                    <!-- Actions -->
                                    <div class="mt-auto d-grid gap-2">
                                        <!-- Bouton ECHANGER -->
                                        <a class="btn btn-tt-primary w-100"
                                            href="/echanger?my_object_id=<?= urlencode($baseObjet['id'] ?? 0) ?>&target_object_id=<?= urlencode($objet['id']) ?>">
                                            <i class="bi bi-arrow-left-right me-1"></i>Échanger
                                        </a>

                                        <!-- Optionnel : un lien discret pour voir détail si tu veux -->
                                        <a class="btn btn-tt-ghost w-100"
                                            href="/objet/<?= urlencode($objet['id']) ?>">
                                            <i class="bi bi-eye me-1"></i>Voir
                                        </a>
                                    </div>

                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>

                <?php endif; ?>
            </div>

        </div>
    </main>

    <?php include('inc/footer.php') ?>

    <div class="toast-container position-fixed bottom-0 end-0 p-3" id="ttToastContainer"></div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="../assets/js/app.js"></script>
</body>

</html>